import React from 'react';

/**
 * TEMPORARY STUB — Phase 2 recovery.
 *
 * The original FloatingOrbOverlay was a draggable floating orb (PanResponder +
 * react-native-reanimated, reading from AssistantContext). It has NOT been
 * reconstructed yet. This placeholder exists only so that the import in
 * `app/_layout.tsx` resolves and `tsc --noEmit` passes module resolution.
 *
 * It renders nothing and has no dependencies other than React.
 * Replace with the real component in a later phase.
 */
export function FloatingOrbOverlay() {
  return null;
}
